[project]
name = "ai-canvas-flow-backend"
version = "0.1.0"
description = "AI Canvas Flow 后端服务"
requires-python = ">=3.12"

[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
